# Layout scrutiny — 2026-05-03 (README hero logo addition)

**Scope:** narrow pass against commit `7e1f297` only. PDF not rebuilt
because no source under `paper/main.{md,tex}` or `paper/figures/fig*`
changed; per the layout-scrutinizer prompt §1.2, scrutinising a stale
PDF is forbidden, and there is no new PDF-affecting input here.
Stage-4 owner therefore restricted this pass to the surfaces the
commit actually touched: `README.md`, the renamed/re-encoded
`paper/figures/logo-obscurity-is-dead.png`, and the references to
that asset in `paper/main.{md,tex}` and `paper/figures/README.md`.

## Checks performed

1. **Asset integrity.** `paper/figures/logo-obscurity-is-dead.png`
   exists, is a valid PNG at 1408×768, 8-bit RGB, non-interlaced,
   1.63 MB on disk — matches the commit message claim (no crop, full
   visual identity preserved). The "CONCEPT:" footer line is part of
   the original 1408×768 frame and is preserved by the no-resize
   re-encode.
2. **Path resolution from `paper/main.md` (line 618) and
   `paper/main.tex` (lines 2253, 2256).** Both reference
   `paper/figures/logo-obscurity-is-dead.png` as the canonical path;
   that path resolves. Captions and prose around the references are
   unchanged by this commit, so rule-11 mirror discipline between
   `main.md` and `main.tex` is unaffected.
3. **`paper/figures/README.md` Rule-14 inventory (line 53).** Entry
   for `logo-obscurity-is-dead.png` is intact and still labels the
   asset as externally generated by Google Gemini, 2026-05-02. No
   contradiction with the commit.
4. **`README.md` hero block (lines 1–3).** A `<p align="center">`
   wrapper around a single `<img>` element with `src`, `alt`, and
   `width="720"`. GitHub renders both `<p align="center">` and inline
   `<img>` natively; nothing in the snippet is markdown that would
   render raw. Alt text is present and discloses Gemini provenance
   per rule 1. Width 720 px is sensible for a 1408×768 source on a
   ~960 px GitHub README content column (no upscaling, ~½ native
   resolution, will downscale cleanly on hi-DPI displays). The
   wrapper sits above the existing `# Obscurity Is Dead` H1 so it
   does not displace the heading anchor used by deep links.
5. **No new defects introduced.** Reviewed `README.md` lines 1–30 in
   context: hero block, H1, tagline, TL;DR, badges row, author block,
   visual-abstract section. The hero does not collide with the
   `fig11-eight-practices.svg` visual abstract further down (rule 15
   hero hierarchy preserved — Gemini logo is the project mark; the
   eight-practices SVG remains the *content* visual abstract).

## Stale upstream defects flagged for upstream agents (no edits made)

- **`docs/handbacks/layout-defect-registry.md` LAY-12** and
  **`docs/handbacks/layout-to-illustrator.md` LAY-12** already record
  that the shattered-jar logo is the final Gemini artwork and that
  the *intact-jar companion* is the only remaining placeholder.
  Those entries are **not** stale after this commit — they correctly
  describe the post-commit state. No close-out needed.
- No other handback entry referenced "missing README hero" by ID, so
  no candidate stale entries to flag for the writer/illustrator.

## Verdict

No defects found. No edits made to `paper/main.{md,tex}` or any
figure script. Registry untouched per scrutinizer scope discipline.

RE-SCRUTINY REQUIRED: no
