# Hand-back: documentation-consistency sweep — 2026-05-04

**Agent:** Claude Opus 4.7 (documentation-consistency sub-agent).
**Branch:** `claude/check-illustration-pipeline-Jqst3`.
**Authoritative spec:** `CLAUDE.md` rules 4, 10, 11, 13, 15; orchestrator
dispatch dated 2026-05-04 ("update all documentation afterward" after
the long pipeline-fix / illustrator-rounds / writer-§8 / redaction
session).
**Authority boundary:** meta-documentation only. No edits to
`paper/main.{md,tex}` (writer-owned); no edits to `paper/figures/*`
(illustrator-owned); no edits to `docs/redaction-policy.md`,
`docs/git-history-rewrite-plan.md`, or
`docs/redaction-audit-2026-05-03.md` (redaction-agent-owned, finalised).
Rule 13 honoured (no `make arxiv`, no public-mirror push).

---

## Files touched

| File | Change | Diff size |
|------|--------|-----------|
| `docs/logbook.md` | Appended one new dated session entry covering the full 2026-05-04 arc (pipeline fix, illustrator rounds 5 & 6, layout round 4, writer §8 strands + FAIR4AI, redaction audit, redaction execution, this consistency sweep). | +15 lines |
| `README.md` | (1) Added a *§8 ask* paragraph in the "What is this?" section summarising the four conclusion strands and the FAIR4AI proposal at the README's flashier register (rule 15). (2) Added a new top-level *Status — public-mirror readiness (2026-05-04)* table after License & FAIR, capturing the four-row done / plan-filed / pending / blocked snapshot. | +12 lines |
| `docs/handbacks/doc-sync-2026-05-04.md` | New (this file). | +~140 lines |

No edits to `paper/figures/README.md`: the *Design-system defaults*
section added in `fae586b` is present and authoritative; the figure
inventory table covers fig1–fig16 + both logos; nothing materially
out of date.

---

## Cross-consistency audit

### `paper/main.md` ↔ `paper/main.tex` parity

| Span | md | tex | Verdict |
|------|----|-----|---------|
| §8 Conclusion (overall) | `## 8. Conclusion` at `main.md:654`; intro paragraphs unchanged | `\section{Conclusion}` (label preserved) | clean |
| §8.1 terminological precision | `### 8.1 A terminological precision before the call-to-action` at `main.md:660` | `\subsection{A terminological precision before the call-to-action}` at `main.tex:2572` | clean (parity) |
| §8.2 call-to-action | `### 8.2 Call-to-action: generate the norms while the practice is young` at `main.md:664` | `\subsection{Call-to-action: generate the norms while the practice is young}` at `main.tex:2599` | clean (parity) |
| §8.3 AI-skeptic | `### 8.3 Integrating the AI-skeptic position: from opponents to co-norm-setters` at `main.md:670` | corresponding `\subsection{...}` present | clean (parity, by `grep`) |
| §8.4 FAIR4AI | `### 8.4 A candidate FAIR for AI-Assisted Research extension` at `main.md:676` | `\subsection{A candidate FAIR for AI-Assisted Research extension}` at `main.tex:2663`; `chuehong2022fair4rs` cited at `main.tex:2672`; `rda2024fair4ml` cited at `main.tex:2677`; FAIR4AI working name at `main.tex:2685–2686` | clean (parity, all bibkeys resolve) |
| §8.5 Future work | `### 8.5 Future work` at `main.md:682` | corresponding subsection present | clean |
| §9.5 DLR e-mail clarification | DLR e-mail line in `main.md:725` carries the "contact channel, not institutional endorsement" framing | matching prose in `main.tex` per commit `56c1f84` | clean |
| §10 redaction-precedent paragraph | `main.md:757` carries the `noheton2026powerocean5c8b815` + `noheton2026powerocean_docreadme` citations | `main.tex` mirror present | clean (per Stage 5 round-3 spot-check, no regression) |

**No drift detected.** No hand-back filed to the writer.

### README ↔ paper central-thesis consistency (rule 15)

- **Title / tagline / thesis.** README "Proprietary by Design. Open by AI." / *"AI-assisted reverse engineering as a means to interoperability — and the security nightmare that comes with it."* matches `paper/main.md` §1 framing. No drift.
- **Headline KPIs (12 % / 7 % / 6 %).** README *Headline numbers* table sourced to `paper/main.md` §3.7, §4.7, §5.7, §6.1. Spot-checked: §3.7 reports the Spider Farmer ratio, §4.7 the EcoFlow ratio, §5.7 the meta-process 6 %. Clean.
- **Cross-validation table (Ondilo / Balboa).** README §6.5 surrogate table matches paper §6.5; the only `[REDACTED:repo-path:BALBOA-UPSTREAM-1]` marker in the README is unchanged and still valid per the redaction register.
- **Visual abstract.** README *Visual abstract* still anchors on `paper/figures/fig11-eight-practices.svg` (ILL-05). No retired figure referenced. Figure-gallery `<details>` block references fig3, fig4, fig5, fig9, fig6, fig10, fig13, fig14 — all present in the figure inventory; the deferred set (fig7, fig8, fig12, fig15, fig16, logos) is correctly described as "the full inventory" pointer.
- **§8 reflection (this pass).** New README paragraph names the four-part move (terminology, call-to-action, AI-skeptic invitation, FAIR4AI proposal) and the two precedents (FAIR4RS, FAIR4ML). No KPI invented; no claim that exceeds the paper.
- **Public-mirror readiness callout (this pass).** New README *Status* table summarises the four redaction-pipeline checkpoints in the same wording the redaction-execution hand-back uses; no new claims, only a roll-up of state already documented under `docs/`.

### `docs/sources.md` cluster Q ↔ paper §8.4 ↔ `paper/references.bib`

- `docs/sources.md` cluster Q at line 484 contains L-FAIR-1 (`chuehong2022fair4rs`, `[ai-confirmed]`), L-FAIR-2 (`rda2024fair4ml`, `[ai-confirmed]`), L-FAIR-3 (proposed FAIR4AI, `[needs-research]`).
- Bibkeys `chuehong2022fair4rs` and `rda2024fair4ml` exist in `paper/references.bib` at lines 775 and 794 with full back-pointers to `docs/sources.md` L-FAIR-1 / L-FAIR-2.
- §8.4 in both `main.md` and `main.tex` cites both keys; the L-FAIR-3 `[needs-research]` framing is honoured by the §8.4 closing sentence "if one is formed, or already exists under an adjacent name, the proposal here defers to it" (`main.md:680`).

**Verdict:** clean. No drift, no missing bibkey, no over-claim against the verification status of L-FAIR-3.

### `paper/figures/README.md` — read-only check

- *Design-system defaults* section added in commit `fae586b` (lines 148–195) is present and authoritative; it names `fig11-eight-practices` as the design anchor and codifies the palette / 9 pt floor / hatch-pair / categorical-glyph contract.
- Figure inventory table covers `fig1` through `fig16` plus both logo assets (`logo-obscurity-is-dead.png`, `logo-pandora-jar-intact.png`).
- The 2026-05-03 *pipeline-fix pass* note (lines 23–28) records the FIG-11 closure for fig8 / fig15 / fig16 plus the fig16 inner-cell font lift.
- No edits applied this pass; nothing materially out of date.

---

## Public-mirror readiness checklist (snapshot)

| Item | State | Evidence |
|------|-------|----------|
| Working-tree redaction (paper text, handbacks, vendored bundle, serials, logbook handles) | **done** | Commits `8c2fec4`, `3aca873`, `23a0f1d`, `4697f1c`, `82741be`, `56c1f84`; register in `docs/redaction-policy.md`; full audit `docs/redaction-audit-2026-05-03.md`. |
| New marker types `owner` / `contact` / `internal-url` / `hostname` | **registered** | Commit `ac20649`. |
| Git history rewrite (`git filter-repo --replace-text`) | **plan filed, NOT executed** | `docs/git-history-rewrite-plan.md` (commit `1b02ccb`); execution gated on explicit human consent per rule 13. |
| `client_secret` literal-value history grep | **clean** | Per `docs/handbacks/redaction-execution-2026-05-04.md:148–153`: `git log --all -S 'client_secret' --oneline` confirmed no literal value present. |
| Upstream-repo redaction follow-up | **pending** | `experiments/*/raw_conversations` and rule-4 carve-outs are out-of-scope for the working-tree pass; policy decision needed before public-mirror push. |
| Public-mirror push / Zenodo / arXiv | **blocked** | Rule 13: requires explicit written human consent + completion of the two pending items above. |

---

## TODOs that did not fit this pass

- **Deeper README restructure.** The README still presents *Headline numbers* and *Cross-validation* tables prominently above the figure gallery; if the public-mirror push is delayed long enough that further KPI revisions land, the table-vs-paper mapping should be re-spot-checked. Out of scope for the current sync (no KPI changed in the §8 strands pass).
- **`docs/fair.md` review against the FAIR4AI proposal in §8.4.** `docs/fair.md` predates the FAIR4AI naming and is currently scoped to the *paper itself* mapped against canonical FAIR. A future writer pass may want to add a short forward-pointer paragraph to §8.4 ("we additionally propose…") so a reader landing in `docs/fair.md` discovers the FAIR4AI conversation. Not done here because edits to `docs/fair.md` are arguably writer-owned given its citation density.
- **`docs/methodology.md` / `docs/submission-plan.md`.** Read-only check this pass: neither file references §8.4 or FAIR4AI. They remain internally consistent with the rest of the paper. Not edited.
- **README hero illustration prompt.** Per rule 15 the README is allowed to commission new visual assets via `docs/prompts/illustration-prompt.md`. The current pass deliberately did not add any new visual asset because the §8 strands are textual / framework-level rather than diagrammatic; if the human author wants a FAIR4AI mapping diagram as a README hero accompanying the §8 paragraph, that is an illustrator pass, not a doc-sync pass.

---

## Re-scrutiny verdict

`RE-SCRUTINY REQUIRED: no` — this is a consistency-sync pass, not an
evaluative pass. The standing layout-loop verdict
(`RE-SCRUTINY REQUIRED: no` after round 4) and readability-loop
verdict (`RE-SCRUTINY REQUIRED: yes` for the next writer pass) are
unchanged.

## Next step (orchestrator)

Await human author consent for (a) the git history rewrite per
`docs/git-history-rewrite-plan.md` and (b) the upstream-repo
redaction follow-up before any public-mirror push. Independently,
Stage 2 writer can pick up the standing path-bullet cluster
(LAY-09 / LAY-22 / LAY-25 / LAY-29) and the carry-over readability
defects (RDB-37 §4.6 sentence split + optional RDB-38).

---

*Hand-back filed 2026-05-04 by the documentation-consistency agent
(Claude Opus 4.7) on branch
`claude/check-illustration-pipeline-Jqst3`.*
