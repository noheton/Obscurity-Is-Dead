# Layout Defect Registry — paper/main.pdf

- **PDF SHA-256:** `ba538ea0d2df9a582889eb16de84d3cd1c6bcf5ae00e647549b7b68bcb2b9e4f`
- **PDF size:** 1,043,497 bytes; **40 pages**
- **Build timestamp:** 2026-05-02T14:53:23Z (`make -C paper pdf`, latexmk + pdfTeX 1.40.25)
- **Source under inspection:** `paper/main.tex` (2,391 lines), mirrored in `paper/main.md` (650 lines)
- **LaTeX log:** `paper/main.log` (Overfull/Underfull/Warning/undefined extracted)
- **Scrutinizer:** Stage 4 — Layout Scrutinizer, 2026-05-02
- **Method note:** the project's bundled PDF viewer rejected the local path
  (no allowed directories registered). Visual sweep performed instead with
  `poppler-utils` (`pdftotext -layout`, `pdfinfo`) operating purely on the
  local artifact — no upload to any external service (CLAUDE.md rule 13).
  Geometric-overflow evidence is anchored on the LaTeX log's
  `Overfull \hbox` measurements; visual-text wraps and layout collisions
  are anchored on the layout-preserving text extraction. Pages that
  require pixel-level inspection (figure-internal contrast, legend
  luminance, kerning of `\texttt{}` runs) are flagged with a `viewer-blocked`
  note in their entries; a pixel-level re-sweep is queued for the next
  Layout Scrutinizer run, see logbook 2026-05-02.

## Defects

| ID | Page | Region | Defect class | Severity | Owner | Source span | Suggested fix |
|----|------|--------|--------------|----------|-------|-------------|---------------|
| LAY-01 | 31 | mid-page, full width, §7.15 first paragraph | broken-cref (`??`) | H | writer | `main.tex:1853`; mirrored at `main.md:545–547` | `\cref{sec:scope-non-goals,sec:synthesis-limits,sec:disc-validity}` references two labels that do not exist in `main.tex` (`sec:scope-non-goals`, `sec:disc-validity`). `pdftotext` shows the rendered string "distributed across ???? and section 6.4 …". Either (a) introduce missing `\label{sec:scope-non-goals}` at `main.tex:178` (the existing `\label{sec:scope}` block) and `\label{sec:disc-validity}` at `main.tex:1415` (currently labelled `sec:disc-threats`), or (b) rewrite the `\cref` to use the labels that *do* exist (`sec:scope`, `sec:disc-threats`). Mirror the choice in `main.md:545–547`. |
| LAY-02 | 7 | upper half, full width, table within §3.4 | table-overflow + cell-wrap-misalignment | H | writer | `main.tex:373–393` (the `tabularx`); mirrored at `main.md:118–141` | Rendered reconciliation table in `pdftotext` shows: row "Dynamic IV source" cell `[6:20]+00 00` wraps and bleeds the trailing `00` into the next column; rows "LED key/IV" and "PS-10 key/IV" emit the `BkJu61kL.../2AKVNUbU...` / `lVIlATSl.../84Rf7SUk...` strings on a *separate visual row* under "HA const.py" rather than aligned to their semantic row. Log shows seven `Overfull \hbox` warnings at `lines 393--393` (range 1.4–47.2pt). Re-author as a `tabularx` with explicit `\raggedright` `X` columns (or shrink to `\footnotesize`), and split the long base64-style fingerprints across two visual lines using `\par` or `\\` *inside* the cell so wrap respects column boundaries. |
| LAY-03 | 12 | upper half, bullet list under §5.2 | margin-overflow (text past right margin) | H | writer | `main.tex:641–651` (Conversation transcripts bullet) and `main.tex:670–676` (build-pipeline / mirroring-rule bullets); mirrored at `main.md:253–264` | Log: `Overfull \hbox (168.80177pt too wide)` at `lines 641--651` and `(123.89566pt too wide)` at `lines 670--673`. `pdftotext` confirms the rendered line `experiments/paper-meta-process/r` truncates with `(copy&paste, web)/` cascading to the next line, with the path running visibly past the text-block right edge. The unbroken `\texttt{}` paths `experiments/paper-meta-process/raw_conversations (copy&paste, web)/` and `claude/develop-paper-structure-7lG2s` exceed line width. Insert `\allowbreak` or `\seqsplit{}` (already loaded? if not, prefer `\path{}` from the `url` package, which is loaded by `hyperref`) inside long `\texttt{}` runs, or rewrite the bullet to break the path across `\texttt{}` segments at `/` boundaries. Mirror in `main.md:253–264`. |
| LAY-04 | 17 | mid-page, bullet list under §6.5 | margin-overflow | H | writer | `main.tex:989–1004`; mirrored at `main.md:359–373` | Log: `Overfull \hbox (46.04323pt too wide)` and `(3.9011pt too wide)` at `lines 989--1004`. The unbreakable `\texttt{T-OND-1..T-OND-10}` and `\texttt{T-BAL-1..T-BAL-12}` identifiers, plus the `docs/prompts/iot-integrator-prompt.md` path, force the line past the margin. Wrap the long path in `\path{}` and insert a `\,` before the test-case ranges so they may break. Mirror in `main.md:359–373`. |
| LAY-05 | 14 | full-page float, Figure 7 (verification-status pipeline) | float-overflow (figure exceeds `\textwidth`) | H | illustrator | `main.tex:805–827` (figure environment), asset `paper/figures/fig9-verification-pipeline.pdf` and script `paper/figures/fig9-verification-pipeline.py` | Log: `Overfull \hbox (226.22418pt too wide) in paragraph at lines 808--827`. This is the largest geometric overflow in the document — the figure box is rendered ~7.97 cm wider than the printable area. Either regenerate the figure at a tighter aspect ratio (currently 730×386pt requested as 434.85pt wide; the *content* layout, not the include-graphics width, is the cause — the inner two-track legend exceeds the bounding box) or rotate the figure with `sidewaysfigure`, or split into two stacked sub-figures (literature track / artifact track). Aspect-ratio retarget in the script is preferred; do not paper over with `\resizebox{\textwidth}{!}`. Joint with writer (`see layout-to-writer.md#LAY-05`) only if the float environment needs a `[t]` placement reflow afterwards. |
| LAY-06 | 19 | upper-third, scoring sub-table inside Figure 8 | table-overflow inside float | H | illustrator | `main.tex:1070–1096` (Figure 8 / fig12-difficulty-taxonomy include); script `paper/figures/fig12-difficulty-taxonomy.py` | Log: `Overfull \hbox (8.79628pt too wide) at lines 1072--1073` ("Composite") and `(2.52565pt) at lines 1070--1096`. `pdftotext` shows the heat-map rows split across what should be a single horizontal stripe per case; "Med High" wraps onto the EcoFlow row. The Composite header overlaps the rightmost rating column. Re-render the figure at the requested 416.7pt × 252.5pt with the cell labels in a smaller font and the column separation widened, or move the composite column to a separate sub-panel. |
| LAY-07 | 7 | redaction tags inline | redaction-rendering | M | writer | `main.tex:412–422`; mirrored at `main.md:145–148` | Log: `Overfull \hbox (40.73097pt too wide) at lines 414--422` and `(55.48029pt) at lines 430--445`. The `\texttt{[REDACTED:username:S-SF-5-username]}` and `\texttt{[REDACTED:credential:S-SF-5-password]}` markers are unbreakable single tokens. Per CLAUDE.md rule 12 the markers must remain literal; the fix is to permit hyphenation inside the redaction tag (e.g. `\seqsplit{[REDACTED:credential:S-SF-5-password]}` or break the rendering with `\nolinkurl{}` from `hyperref`) so the line can break at `:` boundaries. Do **not** alter the redaction text itself. |
| LAY-08 | 9 | mid-page bullet under §4.4 | margin-overflow | M | writer | `main.tex:534–547`; mirrored at `main.md:201–206` | Log: three Overfull warnings (17.98pt, 102.57pt, 105.99pt, 6.24pt) at lines 534--547. Long `\texttt{ACTION_W_CFG_BACKUP_REVERSE_SOC}` symbol and the comma-chained writeable-entity list (`button.system_reboot`, …) drive overflow. Recommend a two-column or nested `itemize` structure or wrap the symbol list as a centered `lstlisting`. Mirror in `main.md`. |
| LAY-09 | 11 | bullet block §5.2 | margin-overflow (multi-paragraph) | M | writer | `main.tex:639–680` (multi-bullet block of long `\texttt{}` paths); mirrored at `main.md:253–272` | Log: cluster of seven Overfull warnings at lines 639–680 (4.85, 118.88, 168.80, 11.47, 86.39, 32.97, 28.97, 56.22, 123.90, 91.47, 26.10pt). Single root cause: every bullet ends with one or more long `\texttt{...}` paths or filenames. Pair with **LAY-03** (a single project-wide pass converting `\texttt{...}` to `\path{...}` for filesystem paths is the right unit of work). |
| LAY-10 | 35 | top, §10 numbered list of eight practices | margin-overflow | M | writer | `main.tex:1979–2028`; mirrored at `main.md:613–625` | Log: Overfull at lines 1979–1982 (50.18pt), 1997–2008 (168.71pt), 2009–2028 (35.69pt + 136.28pt). Cause: long path `experiments/paper-meta-process/raw_conversations (copy&paste, web)/T1-paper-structure-and-literature.md` and `paper/figures/logo-{obscurity-is-dead,pandora-jar-intact}.png` runs. Same fix shape as LAY-03/-09. |
| LAY-11 | 32 | bottom of preceding page (lines 1138–1144) | margin-overflow | M | writer | `main.tex:1138–1144` ("day Cognito refresh tokens dominate the residual-risk picture in both `RESEARCH-PROTOCOL.md`"); mirrored at `main.md` (search §6.7 / §7.13 region) | Log: Overfull `(7.79pt)`. Minor; rebreak sentence around the `\texttt{RESEARCH-PROTOCOL.md}` token. |
| LAY-12 | 36–38 | front matter of §10 illustrations | placeholder-pending (logo assets are explicit AI-authored placeholders) | M | illustrator | `paper/figures/logo-pandora-jar-intact.png` (page 37, included at `main.tex:2191`) and `paper/figures/logo-obscurity-is-dead.png` (referenced in §10 prose at `main.tex:2009–2028`); generation script `paper/figures/logo-placeholders.py` | Both logo PNGs are AI-authored placeholders pending the Google-Gemini-generated final artwork commissioned 2026-05-02 (declared in §10 prose, log shows the inclusion as `figures/logo-pandora-jar-intact.png, id=1799, 343.28pt x 341.11pt`). This is **not** a content-quality or contrast failure — the prose explicitly labels the asset as a placeholder per CLAUDE.md rule 1. Filed as a tracking entry so the illustrator pass replaces the PNG once the Gemini deliverable arrives, and the Layout Scrutinizer re-runs against the final asset. **Do not edit `logo-placeholders.py`.** |
| LAY-13 | 22, 23 | full-width figures (Figure 11 dual-use, Figure 12 threat-models) | pdf-version-incompatibility | M | illustrator | `paper/figures/fig6-dual-use.pdf`, `paper/figures/fig7-threat-models.pdf` (included at `main.tex:1281` and `:1288`) | `pdfTeX` warning: `PDF inclusion: found PDF version <1.7>, but at most version <1.5> allowed`. The figures embed at the wrong PDF version for the document's pdfTeX target. No visible defect today, but a future arXiv build with a stricter pdfTeX may downgrade or reject these floats. Re-export from the source SVG (or `gs -sDEVICE=pdfwrite -dCompatibilityLevel=1.5`) to PDF 1.5. |
| LAY-14 | 1 | author affiliation block | font-shape-fallback | L | writer | `main.tex:1896` region | Log: `Font shape T1/lmtt/bx/n in size <10.95> not available; T1/lmtt/b/n tried instead`. Cosmetic; bold-typewriter glyphs auto-substituted. No reader-visible failure. Optional fix: load `lmodern` `\renewcommand` for `\ttdefault` or avoid `\textbf{\texttt{...}}` in the affected span. |
| LAY-15 | 18, 36, 37 | three floats | float-placement-relaxed | L | writer | `main.tex:1136`, `main.tex:1256`, `main.tex` Pandora-jar float | Log: three `LaTeX Warning: 'h' float specifier changed to 'ht'`. Floats requested with `[h]` were promoted to `[ht]` automatically. Cosmetic — purely a placement-hint downgrade. |
| LAY-16 | 39–40 | bibliography | underfull-hbox (justification rivers) | L | illustrator/writer (joint, advisory) | `main.bbl` lines 9–62 (auto-generated from `references.bib`) | Log: ~25 Underfull `\hbox` warnings in `main.bbl` at the entries for `p0rigth_spiderblebridge`, `noheton_pythonspider`, etc. Cause: long unbreakable URLs (`https://github.com/[REDACTED:repo-path:SF-IMPL-2]`, `experiments/spider-farmer/original/doc/[REDACTED:repo-path:SF-IMPL-2]-master.zip`). Bibliography entries are auto-generated; the practical fix is to wrap long URLs in `\url{}` (already done partially — verify in `references.bib`) and add a `BREAK` directive or `\seqsplit` for the vendored-archive paths. Cosmetic but will recur on every build until addressed. |
| LAY-17 | 8, 11 | KPI summary tables | table-no-toprule-anchor | L | writer | `main.tex:428–445` (Spider Farmer table) and `main.tex:578–590` (EcoFlow table) | Log: Overfull `(55.48pt)` at lines 430–445 and `(113.47pt)` at lines 579–590 *inside the table environment*. Visual cause: the `Total AI-assisted` row's caption text plus the `\textbf{$\sim$10.5\,h}` cell push the rightmost column past the implicit column width. Either widen the third column or shorten the "Key event" cells. Cosmetic relative to LAY-02. |
| LAY-18 | 14 | end of §5.7 | KPI-table column-width pressure | L | writer | `main.tex:863–882` (Underfull cluster) | Log: six Underfull `\hbox` warnings (badness 1308–10000) in lines 863–882. Justified prose has visible "rivers" between the run of citations. Cosmetic. |

## Summary

- **Total defects:** 18 (LAY-01 .. LAY-18)
- **By severity:** H = 6 (LAY-01..LAY-06); M = 7 (LAY-07..LAY-13); L = 5 (LAY-14..LAY-18)
- **By owner:** writer = 11; illustrator = 5 (LAY-05, LAY-06, LAY-12, LAY-13, LAY-16-shared); joint advisory = 1 (LAY-16); informational placeholder = 1 (LAY-12)
- **Most consequential defect:** **LAY-01** — broken `\cref{sec:scope-non-goals,sec:disc-validity}` rendering as `????` on page 31, mid-paragraph in §7.15 — the reader literally cannot find two of three cited limitations sections.

## Mirror-discipline (rule 11) check

Every writer-owned entry above cites both `main.tex` lines and the mirrored
`main.md` lines so a remediating writer can fix in a single mirrored edit.
No drift between `main.tex` and `main.md` was discovered during the scrutiny;
the structural section count matches.

## Redaction (rule 12) check

The PDF rendering of `[REDACTED:username:S-SF-5-username]` and
`[REDACTED:credential:S-SF-5-password]` on page 7 was inspected. The
markers are present and intact; no live credential leaked through the
build. The defect filed (LAY-07) is geometric, not redactional.

## RE-SCRUTINY REQUIRED: yes — six H-severity entries (LAY-01 broken `\cref`, LAY-02 reconciliation-table mis-alignment, LAY-03/-04 margin overruns past `\textwidth`, LAY-05 figure 7 textwidth overflow, LAY-06 figure 8 sub-table overflow) prevent a clean reading of headline evidence; rebuild and re-sweep required after writer + illustrator passes consume the hand-back files.
